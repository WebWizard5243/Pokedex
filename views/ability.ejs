<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/content.css">
</head>
<body>
    <%- include("partials/header.ejs") %>
<% if(locals.ability) { %>
    <h2><%=ability.name.slice(0,1).toUpperCase() + ability.name.slice(1,ability.name.length)%></h2>
    <p class="ability-info"><%= ability.effect_entries[1].effect %></p>
    <form action="/red" method="post">
        <input type="submit" class="goBack-btn" name="" id="" value="Go Back">
    </form>
    <% } %>
</body>
</html>
